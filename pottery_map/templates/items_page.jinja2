{% extends "page.jinja2" %}
{% set title = "Items" %}
{% set root = "./" %}
{% block body %}
    <h1>{{ title }}</h1>

    <div class="wares gy-2" id="wares">
        <div class="d-flex flex-wrap gap-3 items-container">
            {%- for item in items %}
                <div class="item border p-1 d-flex flex-column" id="{{ item['id'] }}">
                    <h4>{{ item['design'] }}</h4>
                    <h5>
                        <a href="{{ root }}companies/{{ make_id(item["company"]) }}.html">{{ item["company"] }}, {{ item["factory"] }}</a>
                    </h5>
                    <p>{{ item['material'] }}</p>
                    <p>{{ item['era'] }}</p>
                    {%- for note in item["notes"] %}
                        <code>{{ note }}</code>
                        <br>
                    {%- endfor %}
                    <div class="mt-auto mx-auto pt-1">{% include "carousel.jinja2" %}</div>
                </div>
            {%- endfor %}
        </div>
    </div>
{% endblock body %}

{% block scripts %}
    {{ super() }}
    <script src="https://cdn.jsdelivr.net/npm/bs5-lightbox@1.8.5/dist/index.bundle.min.js"></script>
{% endblock scripts %}
