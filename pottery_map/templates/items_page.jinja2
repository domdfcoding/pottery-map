{% extends "page.jinja2" %}
{% set title = "Items" %}
{% set root = "./" %}
{% block body %}
    <h1>{{ title }}</h1>

    <div class="wares gy-2" id="wares">
        <div class="d-flex flex-wrap gap-3 items-container">
            {%- for item in items %}
                <div class="item border p-1" id="{{ item['id'] }}">
                    <h3>{{ item['design'] }}</h3>
                    <h4>
                        <a href="{{ root }}companies/{{ make_id(item["company"]) }}.html">{{ item["company"] }}, {{ item["factory"] }}</a>
                    </h4>
                    <p>{{ item['material'] }}</p>
                    <p>{{ item['era'] }}</p>
                    {%- for note in item["notes"] %}
                        <code>{{ note }}</code>
                        <br>
                    {%- endfor %}
                    {% include "carousel.jinja2" %}
                </div>
            {%- endfor %}
        </div>
    </div>
{% endblock body %}

{% block scripts %}
    {{ super() }}
    <script src="https://cdn.jsdelivr.net/npm/bs5-lightbox@1.8.5/dist/index.bundle.min.js"></script>
{% endblock scripts %}
